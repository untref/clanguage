#include <stdio.h>
#include <stdlib.h>

#include "Variant.h"

int main()
{
    Variant instanceOfVariant;      // It might have rubbish so you have to initialize

    initialize( &instanceOfVariant);


    // Variant variantWithInteger
    // Variant variantWithFloat
    // Variant variantWithArray

    // Here you have to print the contente of the variant
    // For the porpouse of the TP0 you have to define several
    // instances of variants then print them alll.



    return 0;
}
