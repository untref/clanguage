#include <stdio.h>

int main( int arguments_count, char** arguments) {
    // Implementar un TDA que simule un ARRAY (el array contendra un solo dato por ves)
    // Pensar q podemos agregar y eliminar elementos.
    //
    // Tratar de resolver el ejercicio sin utilizar REALLOC
}
